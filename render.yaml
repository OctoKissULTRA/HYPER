services:
  - type: web
    name: hyper-trading-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALPHA_VANTAGE_API_KEY
        value: RXV371OVWXK6BQ17
    healthCheckPath: /health
    plan: starter
